function sort(arr){
	var len = arr.length,
		tmpArr = new Array(len),
		inc = 2;
	
	while(inc <= len)
	{
		for(var i = 0, inc = 1, st, mid, end; i < len; ){
			st = i;
			end = Math.min(stB + inc, len);
			mid = Math.min(st + inc, len);		}
		
		inc <<= 1; 
		
	}
	
}

